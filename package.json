{
  "name": "sistem-klinik",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "node scripts/free-dev-ports.js && npm --prefix apps/backend run dev",
    "dev:backend": "node scripts/free-dev-ports.js && npm --prefix apps/backend run dev",
    "build": "npm run build:packages && npm run build:backend",
    "build:packages": "npm --prefix packages/types run build && npm --prefix packages/config run build && npm --prefix packages/database run build",
    "build:backend": "npm --prefix apps/backend run build",
    "start": "npm --prefix apps/backend run start",
    "lint": "npm --prefix apps/backend run lint",
    "test": "npm --prefix apps/backend test -- --runInBand",
    "docker:up": "docker compose -f docker/compose.yml up --build",
    "docker:dev": "docker compose --env-file docker/dev.env -f docker/compose.yml -f docker/compose.override.yml up --build",
    "docker:down": "docker compose --env-file docker/dev.env -f docker/compose.yml down"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.64.0",
    "@typescript-eslint/parser": "^8.64.0",
    "eslint": "9.39.5",
    "tsx": "^4.21.0"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
